const config = {
    DB: {
		DB_USER:,
		DB_PWD:,
		DB_DB:,
		DB_HOST:,
		DB_PORT:3306,
		DB_DIALECT:'mysql'
	},
	REDIS: {
        HOST: ,
        PORT: ,
        AUTH: ,
        SELECT_DB: 4 //用redis的第几个数据库（从索引0开始）
    },
}
module.exports = config;