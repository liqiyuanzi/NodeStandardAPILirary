const config = {
    DB: {
		DB_USER:用户名,
		DB_PWD:密码,
		DB_DB:db,
		DB_HOST:host,
		DB_PORT:3306,
		DB_DIALECT:'mysql'
	},
	REDIS: {
        HOST: "host",
        PORT: port,
        AUTH: auth,
        SELECT_DB: 4 //用redis的第几个数据库（从索引0开始）
    },
}
module.exports = config;